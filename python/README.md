# ragmail Python Project

This directory contains the Python package, dependency metadata, and lockfile for ragmail.

From the repo root:

```bash
uv venv
UV_PROJECT_ENVIRONMENT=$PWD/.venv uv sync --project python --extra dev
```

The Python package source lives under `python/lib/ragmail`.
